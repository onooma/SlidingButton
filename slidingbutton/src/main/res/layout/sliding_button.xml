<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <android.support.v7.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/buttonHeight"
        app:cardBackgroundColor="@color/colorBG"
        app:cardCornerRadius="@dimen/buttonHeightHalf"
        app:cardElevation="0dp">

        <RelativeLayout
            android:id="@+id/rootView"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/circleImageView"
                android:layout_width="@dimen/circleHeight"
                android:layout_height="@dimen/circleHeight"
                android:layout_margin="@dimen/circleMargin"
                android:padding="@dimen/circlePadding"
                android:background="@drawable/circle"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_keyboard_arrow_right_24dp" />

            <TextView
                android:id="@+id/textView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="Medium Text"
                android:textColor="#fff"
                android:textSize="16sp" />

        </RelativeLayout>

    </android.support.v7.widget.CardView>


</RelativeLayout>